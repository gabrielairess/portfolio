# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Dependencies (if you later add a build step)
node_modules/

# Reference only — the Framer export the design is based on (kept locally, not in repo)
berrielbrands_com.html
